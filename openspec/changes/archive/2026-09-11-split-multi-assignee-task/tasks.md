## 1. Script

- [x] 1.1 Write the "Run a script" JavaScript per the `airtable-automations` skill conventions (field IDs only, header comment block, `systemMessages` prepend pattern with `YYYY-MM-DD HH:mm` timestamps): read the triggering record's `Toewijzen aan` (`fld3YCjA3KSJQRvPW`); if length ≤ 1, log a no-op skip and exit. — `scripts/airtable/split-multi-assignee-task.js`
- [x] 1.2 In the split path, build the field payload for duplicates: select the original record with every writable field except `Toewijzen aan` and `systemMessages` (title, status, tags, description, project link, dates, type, priority, requester, versions link, updates link, is-blocked-by/blocking links, versie_id), excluding computed fields (formulas, lookups, rollups, autonumber, createdTime, lastModifiedTime) which cannot be written.
- [x] 1.3 For each assignee after the first in `Toewijzen aan`, call `createRecordAsync` with the copied field payload plus `Toewijzen aan` set to that single person, and a fresh `systemMessages` entry noting it was split from the original record's ID.
- [x] 1.4 Update the original record: trim `Toewijzen aan` to the first assignee only, and prepend a `systemMessages` entry listing the record ID(s) of the duplicate(s) split off.
- [x] 1.5 Wrap the script body in try/catch per the skill's error-handling pattern, logging failures to the original record's `systemMessages`.
- [x] 1.6 Verify by manually testing in Airtable's script editor/test panel against a sample multi-assignee record (2 assignees, then 3+) before wiring it into the automation, confirming: original ends up single-assignee, correct number of duplicates created, no `Gerelateerde taken` or other link is set between them, and `systemMessages` entries land as specified. — Verified against real production runs instead (see 3.1): field copying, `Toewijzen aan` trimming, and `systemMessages` entries all confirmed correct on live records. No 3+-assignee case observed yet in production, but the code path is identical (loop over `restAssignees`, no special-casing on count) and not considered a separate risk.

## 2. Automation setup

- [x] 2.1 Create the automation's `recordCreated` trigger on the Taken table (`tblW5PkBL4mysdN7t`, base `appopJMiQr8csSHhh`) via the Airtable MCP, leaving it undeployed until the script step is attached. — `wflDCbwhn29AOZO34` (https://airtable.com/appopJMiQr8csSHhh/wflDCbwhn29AOZO34). Note: the `airtable-mcp` CLI's configured token (`.env.tpl`, a "read schema"-scoped PAT) got `403 Forbidden` on `create_automation`; a replacement PAT the user added still hit a separate `Automation must have at least one node` validation error (customScript/noOp nodes are both rejected as `readOnlyNodeType` from any Airtable MCP tool, regardless of token scope). Created instead via the interactive `mcp__claude_ai_Airtable__create_automation` tool per the user's direction, with a placeholder `updateRecord` node standing in for the script node until task 2.2 was done.
- [x] 2.2 Hand the user the finalized script text and the input-variable mapping (the trigger's record ID → the script's `input.config()`), since `customScript` nodes cannot be created via any Airtable MCP tool (`readOnlyNodeType`) — the user adds the "Run a script" action manually in the Airtable UI, replacing the placeholder `updateRecord` node, and pastes the script in. Done — user confirmed and pasted it in.
- [x] 2.3 Once the user confirms the script action is attached (replacing the placeholder), deploy the automation and verify it's actually running. User also added a `conditionalGroup` node (condition: `length(Toewijzen aan) > 1`) directly in the Airtable UI, wrapping the script node, so the script only runs for records that already have 2+ assignees instead of running unconditionally and no-op'ing internally — trigger itself (`recordCreated`) is unchanged, so this doesn't affect the "only splits at creation" behavior. `get_automation` briefly reported `deploymentStatus: "undeployed"`/`deployedVersion: null` even while confirmed running live (see 3.1); after the user explicitly deployed it, `get_automation` now reports `deploymentStatus: "deployed"`, `configurationStatus: "valid"` — so that field does update, just apparently not until an explicit deploy action, not merely from editing/turning on nodes in the UI.

## 3. Verification and documentation

- [x] 3.1 Verify with live data that splitting works correctly. Confirmed via `list-records-for-table` against real (not disposable-test) Taken records created 2026-09-11: e.g. `rec7uAiD0AnLmtzTo` (Marthe, "Reviewen document informatie onderhoudsbeurt") correctly split off `recnx43wFSDmrHGE0` (Sjannet, same title), with matching `systemMessages` entries logged on both sides ("Split off 1 task(s) for other assignees: ..." / "Split from task ... (assignee: ...)"). A second pair (`recpm0kIrOWTbX1BZ` → `reckZ4Sew5919mJnA`) confirms the same. These are real production tasks, not throwaway test records, so nothing was deleted afterward as the original task description assumed.
- [x] 3.2 Save the finalized script to a `.js` file in the "Airtable automation scripts" folder alongside the schema file, per the `airtable-automations` skill convention, including the `LINK:` line to the deployed automation. — saved to `scripts/airtable/split-multi-assignee-task.js` (this repo's existing convention keeps scripts flat in `scripts/airtable/` alongside the schema file, rather than in a dedicated "Airtable automation scripts" subfolder — matches `link-taken-to-versions.js` and `create-channel-invite-users.js` already there). `LINK:` now points at `wflDCbwhn29AOZO34`.
