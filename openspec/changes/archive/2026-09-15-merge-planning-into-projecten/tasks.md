## 1. Pre-flight

- [x] 1.1 Export the `Planning` table (`tblhpZ8bLZxY1r6zY`) to CSV as a backup and verify the file has one row per existing Planning record
- [x] 1.2 Checked via `airtable-mcp list-pages-for-base`: interface "Projecten" (`pbdZZhbV0E8gmXPt6`) has 5 affected pages — "Status review 2026" and "Projectplanning 2026" are sourced directly from the `Planning` table (these are the roadmap views tracked for rebuild in 5.3); "Dashboard", "Projecten timeline", and "Totaal overzicht" show the `Planning` link, `create planning` checkbox, and/or `Impact-effort` lookup as columns on Projecten records and will silently lose those columns once the fields are deleted — see 4.1
- [x] 1.3 Recorded via `airtable-mcp list-records-for-table`: 133 Projecten records, 60 Planning records. 74 Projecten have no linked Planning (confirmed acceptable — they'll simply get blank native planning fields). 1 Projecten record ("P082 - Analytics", `rec8vk3QIvVEqGJls`) has 2 linked Planning records with diverging data; user decided Record A (`recMP3plri5sLzLf1`, the fuller one — Quick Win, Gepland voor 2026 Q1-Q4, review note) is authoritative, Record B (`rec1UxIrEjqepVXpL`) is discarded. So exactly 59 Projecten have real planning data to backfill (58 clean 1:1 + P082-Analytics using Record A); 60th Planning record (P082's Record B) is deleted without being migrated.

## 2. Add native fields to Projecten

- [x] 2.1 Created plain native fields on `Projecten`: `Doorlooptijd (maanden)` (`fldoKdpBdGSQ6RgmT`), `Impact - voor de klant` (`fldZn9JPsPuLAFuZU`), `Impact - voor Picoo` (`fldQfSYnV3oQT9SEh`), `Effort - geld` (`fldL63w5YPwEi3RGF`), `Gepland voor` (`fldozOz790lzNUZrF`, live choices pulled from Planning incl. 2027 Q1/Q3 which weren't in the stale yaml), `Review` (`fldyeS27MRICizgmL`). `Effort - tijd` collided with the existing lookup-via-Planning field of the same name (422 DUPLICATE_OR_EMPTY_FIELD_NAME) — created as `Effort - tijd (nieuw)` (`fldVhFNWnWZUapL4z`); rename to `Effort - tijd` is now folded into 4.3 once the old lookup is deleted. Verified via `get-table-schema`.
- [x] 2.2 Created `Impactscore` (`fldgY3BviIASEoUcL`, `AVERAGE({Impact - voor Picoo},{Impact - voor de klant})`) and `Effortscore` (`fldfKKjnRhjbhwMDt`, `AVERAGE({Effort - tijd (nieuw)},{Effort - geld})`) on `Projecten`, matching Planning's original formulas. Known cosmetic gap: Planning's originals displayed at precision 1 (e.g. 1.5); the API silently ignores an attempt to set formula-result precision on creation/update, so these display at precision 0 (whole numbers) — the underlying computed value used by dependent formulas (e.g. Impact-effort in 2.3) is unaffected, this is display-only and fixable later by hand in the Airtable UI if desired
- [x] 2.3 Created `Impact-effort (nieuw)` (`fldqQCaiDNkDFloqz`) on `Projecten`, reproducing Planning's `Prioriteit` classification formula referencing the new `Impactscore`/`Effortscore`. Named with a `(nieuw)` suffix — as expected, `Impact-effort` collided with the existing lookup field of that name; rename folded into 4.3 alongside the `Effort - tijd` rename. No collision with the manual `Prioriteit` (P1–P4) field, which is a different name entirely. Known cosmetic gap: Planning's original `Prioriteit` rendered as a colored single-select badge (4 defined choices/colors); the API-created formula field returns plain text with the same 4 label strings — functionally identical, just without the colored badge styling, fixable later by hand in the Airtable UI if desired

## 3. Backfill and verify

- [x] 3.1 Backfilled via `list-records-for-table` on `Planning` (60 records, all fields) + `update-records-for-table` on `Projecten` in 2 batches (50 + 9, respecting the 50-record API limit), excluding P082's discarded Record B. 59/59 updates confirmed successful (both batches returned the expected record counts).
- [x] 3.2 Spot-checked 4 Projects (3 random + P082-Analytics) against source Planning values: raw fields, computed Impactscore/Effortscore, and Impact-effort classification all match exactly (P082-Analytics confirmed carrying Record A's values: Impactscore 3, Effortscore 2, Quick Win, Q1-Q4, the "On track" review note)
- [x] 3.3 Confirmed all 60 Planning records accounted for: 59 successfully written to their Project's native fields (3.1), 1 (P082's discarded Record B, `rec1UxIrEjqepVXpL`) intentionally not migrated per the user's decision in 1.3. Clear to proceed to deletion.

## 4. Remove the old structure

- [x] 4.1 User re-added the native `Impact-effort` field to the "Dashboard", "Projecten timeline", and "Totaal overzicht" pages in the Airtable UI.
- [x] 4.2 Deleted the `Create planning record` automation (`wflPlb5ExaZdZkD8z`). The API refused to delete it while deployed ("Turn it off in the Airtable UI first" — no API toggle exists in `airtable-mcp` for deployment status); the user turned it off manually in the Airtable UI, then the delete succeeded via API. Verified: no longer appears in `list-automations`.
- [x] 4.3 `airtable-mcp` has no `delete-field` tool (API/tool limitation, confirmed by trying — only `create-field`/`update-field` exist), so the user deleted `Gepland voor (from Projecten 2026)`, `Impact-effort` lookup, and `Effort - tijd` lookup manually in the Airtable UI; verified via `get-table-schema` that the old field IDs (`fldlTnDiV4QAeWzvA`, `fldMfTbGNzHUl9uri`, `fldlLm8DJOHQViuUE`) are gone. Then renamed `Effort - tijd (nieuw)` → `Effort - tijd` and `Impact-effort (nieuw)` → `Impact-effort` via `update-field`, verified via `list-tables-for-base`. Follow-up: user wants `Impact-effort`'s single-select badge formatting (colors) restored by hand in the UI — the API silently ignores an attempt to set formula-result formatting (same limitation as the precision issue in 2.2), functionally the field already returns the correct 4 label strings.
- [x] 4.4 Deleted by the user in the same manual pass as 4.3 (no `delete-field` API available); verified the `Planning` link field (`fld53HMcGbuIfZRW7`) is gone via `get-table-schema`
- [x] 4.5 Deleted by the user in the same manual pass as 4.3 (no `delete-field` API available); verified the `create planning` checkbox (`fldZHqjw62t2nZ6wc`) is gone via `get-table-schema`
- [x] 4.6 Deleted the `Planning` table (`tblhpZ8bLZxY1r6zY`) via `delete-table`. Verified via `list-tables-for-base`: no longer present.

## 4b. Newly discovered scope: broken cross-table lookups

- [x] 4.7 User repointed both lookups manually in the Airtable UI. Verified via `get-table-schema`: `Effort - tijd (from Project)` (`fldHXhgLVVUGd2eeg`) now points at `fldVhFNWnWZUapL4z` (`isValid: true`), `Gepland voor` (`fldhbeYqZqpcDDT2H`) now points at `fldozOz790lzNUZrF` (`isValid: true`), and `_Suggestie aantal uren` (`fldtGDdxKAACwFWNl`) is valid again.

## 5. Sync and close out

- [x] 5.1 Re-ran the local schema sync. Hit an unrelated pre-existing bug in `../tools/sync-airtable-schema.py` (crashed on a formula field with a null `result`, from an already-broken formula in `Allocaties` unconnected to this change) — fixed the script (`opts.get("result", {})` → `opts.get("result") or {}`, matching the pattern already used two lines below for lookup fields) and re-ran successfully. Verified `airtable-schema-projectmanagement.yaml`: no `Planning` table, all new `Projecten` fields present.
- [x] 5.2 User confirmed in the Airtable UI: the new fields did not get auto-added to "Sync view for Assets". The Assets-base sync is unaffected, as scoped.
- [x] 5.3 User rebuilt "Status review 2026" and "Projectplanning 2026" directly on `Projecten` in the Airtable UI.
